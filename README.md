# Dashboard for validating and correcting peak detection in ecg signals

## Run

`cd ibi_dashboard && npm start`

## Data input format

To convert the ibi data to the format required by the offline dashboard, run 'node ibiDataToInputData'
